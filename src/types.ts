export interface DirectionOptions {
  directionField: string;
  showValue: boolean;
  valueField: string;
  transitionDuration: number;
  maxValue: number;
  tickCount: number;
  tickLength: number;
  tickWidth: number;
}
